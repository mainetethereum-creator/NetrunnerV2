'use client';
import dynamic from 'next/dynamic';
const LandscapePanel=dynamic(()=>import('./LandscapePanel'),{ssr:false});
import type {LandscapeStudio} from './landscape-studio';
import {useEffect,useRef,useState} from 'react';
import Link from 'next/link';
import {POIS,EXTRACTIONS,ITEMS,RULES,type LootKind} from './config';
import {bankLoot,type Bag} from './session';
import type {createExpedition,Snapshot} from './scene';
import GameHud from '../game/GameHud';
import styles from './Expedition.module.css';
import {MICROBUS_VIEW} from './microbus-layout';
import {PROP_ASSETS,type PropId} from './prop-assets';
import type {EditorState} from './prop-editor';
const PropPreview=dynamic(()=>import('./PropPreview'),{ssr:false});
const SAVE_KEY='netrunner.expedition.stash.v1';
type Save={bag:Bag;lastRun:string};
function readSave():Save{try{const data=JSON.parse(localStorage.getItem(SAVE_KEY)??'{}');const bag:Bag={};for(const k of Object.keys(ITEMS) as LootKind[])if(Number.isSafeInteger(data.bag?.[k])&&data.bag[k]>0)bag[k]=data.bag[k];return {bag,lastRun:typeof data.lastRun==='string'?data.lastRun:''};}catch{return {bag:{},lastRun:''};}}
export default function Expedition(){
  const host=useRef<HTMLDivElement>(null),engine=useRef<ReturnType<typeof createExpedition>|null>(null),runId=useRef('');
  const [state,setState]=useState<Snapshot|null>(null),[error,setError]=useState(''),[debug,setDebug]=useState(false),[debugAllowed,setDebugAllowed]=useState(false),[,setStash]=useState<Bag>({}),[saved,setSaved]=useState(false),[autoFire,setAutoFire]=useState(false),[master,setMaster]=useState(false),[category,setCategory]=useState('Все'),[selected,setSelected]=useState<PropId>('barricade'),[rotation,setRotation]=useState(0),[length,setLength]=useState(3);
  const [editorState,setEditorState]=useState<EditorState>({instances:[],selected:null,placing:false,notice:''});
  const [landscapeStudio,setLandscapeStudio]=useState<LandscapeStudio>();
  const changeRotation=(value:number)=>{setRotation(value);engine.current?.editor.setRotation(value);};
  const toggleMaster=(value:boolean)=>{setMaster(value);engine.current?.setMaster(value);};
  useEffect(()=>{let disposed=false;runId.current=crypto.randomUUID();
    import('./scene').then(({createExpedition})=>{if(!disposed&&host.current){setStash(readSave().bag);const params=new URLSearchParams(location.search);setDebugAllowed(params.has('debug'));const created=createExpedition(host.current,setState,setError,next=>{setEditorState(next);const item=next.instances.find(i=>i.id===next.selected);if(item){setSelected(item.asset);setRotation(item.rotation);setLength(item.length??3);}});engine.current=created;setLandscapeStudio(created.landscape);if(params.has('van'))created.teleport(MICROBUS_VIEW);else if(params.has('fence'))created.teleport({x:27,z:34});else if(params.has("vegetation"))created.teleport({x:22,z:55});}}).catch(()=>setError('Не удалось запустить мир. Обновите страницу.'));
    return()=>{disposed=true;engine.current?.dispose();engine.current=null;};
  },[]);
  useEffect(()=>{if(state?.status!=='complete'||saved)return;try{const old=readSave(),next=old.lastRun===runId.current?old:{bag:bankLoot(old.bag,state.bag),lastRun:runId.current};localStorage.setItem(SAVE_KEY,JSON.stringify(next));setStash(next.bag);setSaved(true);}catch{setError('Хранилище браузера недоступно: добыча ещё не сохранена. Освободите место и обновите сохранение.');}},[state,saved]);
  const toggleDebug=()=>{engine.current?.setDebug(!debug);setDebug(!debug);};
  return <main className={styles.root}>
    <div className={styles.scene} ref={host}/>
    <header className={styles.header}><Link href="/">C◇B / CYBERBASE</Link><span>OUTLANDS · EXPEDITION 01</span><Link href="/">Покинуть вылазку без добычи ↗</Link></header>
    <aside className={styles.map}><small>ЛОКАЛЬНЫЙ СИГНАЛ</small><svg viewBox="0 0 144 72" aria-label="Карта открытых мест"><path d="M 3 36 H 140" stroke="#637470" strokeWidth="4"/>{state?.discovered.map(id=>{const p=POIS.find(o=>o.id===id)!;return <circle key={id} cx={p.x} cy={p.z} r="1.6" fill="#d4b47d"><title>{p.name}</title></circle>;})}{EXTRACTIONS.map(e=><rect key={e.id} x={e.x-1.5} y={e.z-1.5} width="3" height="3" fill="#81dbdf"><title>{e.name}</title></rect>)}<circle cx={state?.x??9} cy={state?.z??36} r="1.8" fill="white"/></svg><small>○ ВЫ · ▪ ЭВАКУАЦИЯ · ◇ НАЙДЕННЫЕ POI</small></aside>
    <GameHud bag={state?.bag} />
    <div className={styles.message} role="status">{error||state?.message||'Загружаем материалы и персонажа…'}</div>
    <footer className={styles.controls}><span>WASD · клик: идти · Shift: бег · Space: огонь · E: действие</span><div><button aria-pressed={autoFire} onClick={()=>{engine.current?.setAutoFire(!autoFire);setAutoFire(!autoFire);}}>{autoFire?"Автоогонь: вкл":"Автоогонь: выкл"}</button><button onClick={()=>engine.current?.interact()}>{state?.extraction?`Эвакуация ${Math.ceil(RULES.extractionSeconds-state.extraction)} с`:`E · ${state?.near??'Взаимодействие'}`}</button><button className={styles.masterButton} aria-pressed={master} onClick={()=>toggleMaster(!master)}>MASTER</button>{debugAllowed&&<button onClick={toggleDebug}>DEBUG</button>}</div></footer>
    {master&&<aside className={styles.master} onPointerDown={e=>e.stopPropagation()} onPointerUp={e=>e.stopPropagation()} onKeyDown={e=>e.stopPropagation()}><div className={styles.masterHead}><b>MASTER · РЕДАКТОР КАРТЫ</b><button onClick={()=>toggleMaster(false)} aria-label="Закрыть редактор">×</button></div><p className={styles.masterHint}>WASD · панорама · колесо · масштаб<br/>Esc · отменить размещение · Delete · удалить выбранное</p><div className={styles.masterTabs}>{['Все','Здания','Ресурсы','Детали','Ограждения','Ландшафт'].map(tab=><button key={tab} className={category===tab?styles.activeTab:''} onClick={()=>{setCategory(tab);engine.current?.setLandscapeMode(tab==='Ландшафт');}}>{tab}</button>)}</div>{category==='Ландшафт'?<LandscapePanel studio={landscapeStudio}/>:<><div className={styles.masterObjects}>{PROP_ASSETS.filter(a=>category==='Все'||a.category===category).map(item=><button key={item.id} className={selected===item.id?styles.selectedObject:''} onClick={()=>{setSelected(item.id);engine.current?.editor.select(null);engine.current?.editor.setAsset(item.id);}}>{item.name}</button>)}</div><div className={styles.propPreview}><PropPreview asset={selected} rotation={rotation} length={length}/><small>{PROP_ASSETS.find(a=>a.id===selected)?.description}</small></div><div className={styles.masterTransform}><span>{editorState.selected?'Редактирование экземпляра':'Новый объект'} · <b>{PROP_ASSETS.find(a=>a.id===selected)?.name}</b></span><label>Поворот <input aria-label="Поворот объекта" type="range" min="0" max="360" value={rotation} onChange={e=>changeRotation(Number(e.target.value))}/><em>{rotation}°</em></label>{selected==='modular-fence'&&<><label>Длина <input aria-label="Длина забора" type="range" min="3" max="24" step="3" value={length} onChange={e=>{const n=Number(e.target.value);setLength(n);engine.current?.editor.setLength(n);}}/><em>{length} м</em></label><small>Секции по 3 м · концы стыкуются в радиусе 0,65 м · повторные клики продолжают забор</small></>}<button onClick={()=>changeRotation((rotation+45)%360)}>Повернуть на 45°</button><button className={styles.place} onClick={()=>{engine.current?.editor.setAsset(selected);engine.current?.editor.setLength(length);engine.current?.editor.arm();}}>{editorState.placing?'Кликните по карте · размещение активно':'Разместить на карте'}</button>{editorState.placing&&<button onClick={()=>engine.current?.editor.cancel()}>Завершить размещение</button>}<button disabled={!editorState.selected} onClick={()=>engine.current?.editor.remove()}>Удалить выбранный экземпляр</button><small role="status">{editorState.notice}</small><label>На карте · {editorState.instances.length}/200</label><select aria-label="Выбрать размещённый экземпляр" value={editorState.selected??''} onChange={e=>{const i=editorState.instances.find(i=>i.id===e.target.value);engine.current?.editor.select(i?.id??null);if(i){setSelected(i.asset);setRotation(i.rotation);}}}><option value="">Выберите экземпляр или кликните по нему</option>{editorState.instances.map((i,n)=><option key={i.id} value={i.id}>{n+1}. {PROP_ASSETS.find(a=>a.id===i.asset)?.name} · {i.x.toFixed(1)}, {i.z.toFixed(1)}</option>)}</select></div></>}</aside>}
    {debug&&<aside className={styles.debug}><b>{state?.room}</b><p>{state?.fps} FPS · {state?.draws} draw calls<br/>{state?.activeNPC} NPC · {state?.chunks} chunks</p><Link href="/editor/vegetation">Редактор растительности ↗</Link><button onClick={()=>engine.current?.teleport(MICROBUS_VIEW)}>Заброшенный микроавтобус</button><button onClick={()=>engine.current?.teleport({x:22,z:55})}>Лесной участок</button>{[...POIS,...EXTRACTIONS].map(p=><button key={p.id} onClick={()=>engine.current?.teleport(p)}>{p.name}</button>)}</aside>}
    {state&&state.status!=='active'&&<section className={styles.result}><h1>{state.status==='complete'?'Эвакуация завершена':'Вылазка потеряна'}</h1><p>{state.status==='complete'?(saved?'Добыча сохранена в этом браузере.':'Сохраняем добычу…'):'Содержимое рюкзака потеряно. Склад базы не пострадал.'}</p><p>Уничтожено противников: {state.kills}</p><Link href="/">Вернуться в CyberBase →</Link></section>}
  </main>;
}
